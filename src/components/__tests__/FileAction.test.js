import React from "react";
import ReactDOM from "react-dom";
import { shallow, mount } from "enzyme";

describe("FileAction.js", () => {
  it("empty", () => {
    expect(true).toEqual(true);
  });
});
